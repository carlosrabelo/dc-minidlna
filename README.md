# MiniDLNA (Docker Compose)

## docker-compose

Iniciar:

```
docker-compose -p minidlna up -d
```

Parar:

```
docker-compose -p minidlna down
```

## makefile

Iniciar:

```
make start
```

Parar:

```
make stop
```
